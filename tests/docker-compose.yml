
services:

  auth_service:
    ports:
      - "8000:8000"
    env_file:
      - tests/.docker.env

  tests:
    build: ./tests/
    depends_on:
      - async_api
    env_file:
      - .env
      - .docker.env

  redis:
    ports:
      - "6379:6379"