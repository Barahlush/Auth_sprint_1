version: '3'
services:

  auth_service:
    build: auth_service
    container_name: auth_service
    restart: always
    volumes:
      - ./auth_service:/opt/app
    env_file:
      - .env
    expose: 
      - "8000"